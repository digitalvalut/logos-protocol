<?xml version="1.0" encoding="utf-8"?>
<!--
    Copyright 2026 Associazione di Promozione Sociale DigitalValut (ETS)

    Licensed under the Apache License, Version 2.0 (the "License");
    you may not use this file except in compliance with the License.
    You may obtain a copy of the License at

        http://www.apache.org/licenses/LICENSE-2.0

    Unless required by applicable law or agreed to in writing, software
    distributed under the License is distributed on an "AS IS" BASIS,
    WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
    See the License for the specific language governing permissions and
    limitations under the License.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Two devices find each other and then talk to each other directly, so the
         phone needs the network. Nothing said in a conversation passes through
         anything but the two devices themselves. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Asked for at the moment a call is actually placed or answered, never at
         install time, and refusing them leaves messages and files working. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Writing a received file into Downloads. Not needed from Android 10
         onward, where the same thing goes through MediaStore instead. -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!-- Ringing while the app is closed, and nothing else. There is no push
         service here and no Google Play Services: the phone does its own
         waiting, which is why it needs to be allowed to keep doing it. The
         whole group is inert until somebody turns the feature on. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- What puts an incoming call on the lock screen instead of leaving a
         silent line in a shade nobody looks at. -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <!-- Without it the processor sleeps between one check and the next, and the
         phone rings whenever it happens to wake up — too late to answer. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- A restarted phone should not quietly stop being reachable while the
         setting still reads as on. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Declared, but not required: a phone with no camera still does messages,
         files and voice, and should not be told the app is unavailable. -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/appName"
        android:supportsRtl="true"
        android:usesCleartextTraffic="false"
        android:theme="@android:style/Theme.Black.NoTitleBar">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden|smallestScreenSize|screenLayout|uiMode"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- The least a locked screen can show and still mean something: that
             somebody is calling, and two buttons. Excluded from recents so a
             missed call does not sit in the task switcher afterwards. -->
        <activity
            android:name=".CallActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleTop"
            android:showOnLockScreen="true"
            android:taskAffinity=""
            android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen" />

        <service
            android:name=".RingService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <!-- Android asks what the special use is. The honest answer, and the
                 reason no ordinary type fits: this app has no push service, so
                 the phone has to do its own waiting for an incoming call. -->
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Waiting for incoming peer-to-peer calls without any push service" />
        </service>

        <receiver
            android:name=".BootReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
